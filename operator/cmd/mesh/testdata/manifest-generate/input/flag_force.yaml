apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
spec:
  hub: registry.cn-hangzhou.aliyuncs.com/acejilam
  profile: empty
  components:
    pilot:
      enabled: true
  values:
    global:
      badKey: badValue

