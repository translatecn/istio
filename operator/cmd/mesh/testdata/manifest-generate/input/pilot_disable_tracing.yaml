apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
spec:
  hub: registry.cn-hangzhou.aliyuncs.com/acejilam
  profile: empty
  values:
    global:
      proxy:
        tracer: none
  components:
    pilot:
      enabled: true

