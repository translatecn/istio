templates:
  sidecar: |-
    spec:
      initContainers:
      - name: istio-init
        image: registry.cn-hangzhou.aliyuncs.com/acejilam/proxy_init:unittest-{{.Values.global.tag}}
      containers:
      - name: istio-proxy
        image: registry.cn-hangzhou.aliyuncs.com/acejilam/proxy_debug:unittest
