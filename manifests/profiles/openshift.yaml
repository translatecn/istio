apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
spec:
  hub: ccr.ccs.tencentyun.com/acejilam
  components:
    cni:
      enabled: true
      namespace: kube-system
  values:
    global:
      platform: openshift

